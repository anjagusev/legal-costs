// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
	// If you publish under a subpath (e.g. https://yoursite.com/legal-costs/), set:
	// base: '/legal-costs/',
});
